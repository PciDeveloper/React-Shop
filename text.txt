

리액트에서 많이 쓰이는 문법
1.  let name = 'park';
    let age = 20;

    let person = {
        name : name,
        age : age
    }
    
    let person = { 키와 값을 담은 변수 이름이 같으면 이렇게만 작성해도 가능함
        name,
        age
    }

    console.log(person);